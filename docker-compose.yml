version: "3.8"

services:
  db:
    image: mysql:8.0
    container_name: skinclinic-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: mode321
      MYSQL_DATABASE: clinic_management_db
      TZ: America/Monterrey
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - skinclinic-network
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=mode321
      - DB_NAME=clinic_management_db
      - TZ=America/Monterrey
    depends_on:
      - db
    networks:
      - skinclinic-network

  # ============================================================================
  # SERVICIO FRONTEND (Vite / React)
  # ============================================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_WATCH=true
    command: npm run dev -- --host
    networks:
      - skinclinic-network

networks:
  skinclinic-network:
    driver: bridge
